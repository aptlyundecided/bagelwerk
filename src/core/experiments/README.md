# `src/core/experiments`

No experiments are currently active.

Use this directory only for deliberately scoped Flow / Node proving packages. New experiments are outside the active build/test surface until explicitly wired back in.

Retired pre-Flow/Node experiments were removed during the cleanup; recover them from git history only for historical comparison.
