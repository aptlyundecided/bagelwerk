# Tests

Deterministic tests for small Flow Runner core helpers.

Broader integration-style deterministic runner tests live beside the public Flow Runner surface and the runner-native built-in packages (currently `platform-tour`).
